# quick test file to check integrity

log(4);
cout("sqr test");
*~ a <~ 5;
*~ b <& a;
a <~ 10;
(a == b); # has to be true

*~ c <~ { q <~ 10, w <~ "asd" };
*~ d <~ [ 2, 5, 10, 32 ];

*~ cd <~ c:q + d:2;
(cd == 20);

funq f (j, k {
	return j + k - a;			# <- also tests qontext
});

f(5, 3);

if (a > 1 && cd < 100) {
	if (a > 32) {
		cout("wrong");
	} else {
		cout("correct");
	}
}

funq fib (a, b {
	cout(a + b);
	if (a < 99999) {
		fib(b, a + b);
	}
});

cout("nice!");


